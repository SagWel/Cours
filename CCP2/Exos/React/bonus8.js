// Vous travaillez sur un configurateur de produit personnalisé. 
// Deux tableaux sont fournis : un contenant les couleurs disponibles, un autre contenant les tailles. 
// Votre mission : générer toutes les combinaisons possibles sous forme d’un tableau d’objets, chaque objet représentant une combinaison unique de couleur et de taille.

// Fonction à compléter
function generateCombinations(colors, sizes) {
  // Compléter ici…
}

const colors = ['Rouge', 'Vert'];
const sizes = ['S', 'M'];
console.log(generateCombinations(colors, sizes));